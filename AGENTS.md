# m2bOS Repository Constraints

本文件约束仓库内所有人工和 Agent 变更。当前用户决策优先于历史实现，不得通过兼容层恢复已经删除的架构。

## 当前记忆重构阶段

- 旧长期记忆树、写入链、文档身份、编辑/删除命令、投影和公开接口已经退出；不保留别名、迁移读取、禁用分支、空实现或新旧双轨。
- 新记忆树的 L2 权威目录已经确认：`profile.md`、`preferences/{topic}.md`、`entities/{category}/{name}.md`、`tools/{tool_name}.md`、`events/YYYY/MM/DD/{event_name}.md`、`intentions/{intent_name}.md`。集合目录采用 OpenViking 的目录分层思想，允许保存可重建的 `.abstract.md`（L0）和 `.overview.md`（L1）；`profile.md` 仍是直接读取的单文件 L2。当前实现记忆地址、目录地址、L0/L1/L2 路径、Markdown 原文安全操作、有界枚举和自下向上刷新；不得加入旧实现或兼容入口。
- 新记忆地址协议固定为 `memory://`，其后路径与上述记忆树根目录一一对应，例如 `memory://profile.md`、`memory://preferences/{topic}.md` 和 `memory://events/YYYY/MM/DD/{event_name}.md`；根、合法目录以及目录下 `.abstract.md`/`.overview.md` 也可以被确定性寻址。URI 不包含 owner、用户空间、稳定文档 ID 或旧 Context scope，只接受完整协议形式，不兼容短路径、`viking://` 或 `memoryos://`。URI 解析必须复用 `MemoryAddress`、`MemoryDirectory` 和 `MemoryLevel` 的树约束，禁止产生树外路径或通过静默清洗改名。
- L2 是唯一权威记忆，只能由 Memory Editor 的确定性写入阶段修改。L0/L1 是从直接 L2 文件和子目录 L0 派生的 Serving 内容，必须能够删除并重建；远程向量索引再从 L0/L1/L2 派生，三者都不是新的长期记忆字段。L0/L1 或向量发布失败不得回滚或改写已经成功写入的 L2，但对应 MemoryJob 必须保持未完成并有界重试，直到派生层补齐。
- L2 物理格式统一为 Schema 渲染的 Markdown 正文加文件末尾唯一 `M2BOS_MEMORY_FIELDS` JSON 注释；正文与结构字段属于同一个原子 Markdown 文件。注释顶层只保存可信系统字段 `memory_type`、`revision`、`created_at`、`updated_at`、仅 Intention 使用的 `last_confirmed_at`、业务 `fields` 以及系统确定性维护的 `links`、`backlinks`，其中版本、时间和持久关系不得由 LLM 或内容 Schema 直接生成。每条持久关系只保存规范 `from_uri`、`to_uri` 和受控 `link_type`；源文档的 `links` 与目标文档的 `backlinks` 保存同一关系对象并使用统一身份 `(from_uri, to_uri, link_type)`。结构字段负责确定性更新，正文负责人类阅读和 L0/L1 派生，关系不嵌入正文。读取必须校验 Schema、真实路径、关系方向、真实地址、规范序列化和正文重渲染完全一致；旧裸 Markdown、缺少系统字段、重复注释、宽松 JSON 和双轨写入均不兼容。内部注释和关系元数据不得进入 L0/L1 提示词。
- L2 正文字符上限和完整物理文件字节上限统一由 `MemoryDocumentConfig` 管理，记忆树在写入前和读取后执行同一组检查；语义层不得维护另一套相互冲突的单文件限制。当前 `MemoryTree.write()` 只落盘已经构造完成的 `MemoryDocument`，不读取旧记忆、不执行 YAML 合并策略、不推进版本或生成时间；这些职责只属于后续 Memory Editor。
- 六类长期记忆内容 Schema 使用独立声明，限定字段角色、字段类型、路径模板、Markdown 模板、操作模式和未来合并策略。LLM 结构化解析采用由这六类 YAML 派生的严格 `MemoryCandidateBatch`：顶层固定为 `profile`、`preferences`、`entities`、`tools`、`events`、`intentions` 六个候选数组，以及 `identity_proposals` 和 `relations` 两个临时提议数组；未知顶层字段和未知业务字段均拒绝，`profile` 最多一项。每条记忆候选携带只在本次解析周期有效的临时 `page_id`：系统为完整读取的旧 L2 节点分配 `1..99`，模型为新节点分配不重复的 `100+`，更新旧节点必须复用系统给出的编号；该编号不得持久化为文档身份。Intention 候选额外携带解析期 `confirmed` 布尔值，只表达完整 Conversation 是否明确新建、更新或再次确认该事项；它不得持久化为业务字段或时间戳。`identity_proposals` 只允许 `same_memory` 或 `remove_memory`：前者使用受控 `duplicate_identity` 原因并引用一个完整读取的旧来源编号和一个存活目标编号，后者只能使用 `explicit_forget` 或 `fully_invalidated` 原因并引用一个完整读取的旧编号；它们只是供二次语义审查和确定性身份规划使用的临时提议，不能包含 URI、revision、置信度或耐久操作。`relations` 的每项只允许受控 `ADD` 或 `REMOVE`、两个 `page_id` 和受控关系类型，不能包含最终 URI、反向链接、权重、描述或文本锚点；`REMOVE` 只表示两个节点仍保留而既有关系已经被完整 Conversation 明确否定，不能由“本轮没有再次提到”推导。除临时 `page_id` 和 Intention 的解析期 `confirmed` 外，记忆候选只包含业务字段，不得包含 URI、文件路径、耐久 create/update/delete、revision、时间戳、owner、tag、置信度或 EvidenceSlice；来源 Conversation、旧记忆快照和临时编号映射由系统在模型输出之外绑定，地址、关系解析、合并和耐久操作由 Memory Editor 确定性决定。
- `profile.md` 只回答“用户是谁”，保存职业、背景、长期角色、稳定能力和稳定客观状态；`preferences/{topic}.md` 只回答“用户如何选择”，保存沟通风格、输出格式、工作流习惯、工具偏好以及明确的喜欢或不喜欢。沟通方式、做事习惯和选择倾向不得重复写入 profile；职业身份、客观能力和事实状态不得因为经常出现而写成 preference。
- Conversation 消息的 `occurred_at` 表示“这句话什么时候说的”，`events` 的 `event_date` 表示“事件或状态变化什么时候实际发生”，两者不得混用。明确日期直接使用；相对日期只能基于对应源消息时间确定性换算；在对话中刚作出的决定、确认或承诺使用该状态变化对应的消息日期。过去事件没有可确定日期时不得以对话日期冒充并生成 Event；未来尚未发生且仍需推进的事项进入 Intention，其目标时间不得冒充已经发生的 `event_date`。
- `entities/{category}/{name}.md` 只为具有可识别稳定名称，并且至少存在一条以后可能再次使用的稳定事实或关系的对象建立实体卡片；临时举例、顺带提及或没有可复用事实的普通名词不得生成 Entity。`summary` 必填并承载已知的最小完整事实，`details` 可选；来源信息不足时只写 summary，禁止为了补齐结构而重复 summary、扩写常识或推测细节。
- `tools/{tool_name}.md` 只保存真实工具调用形成的稳定工具知识，不保存调用流水、次数、成功率或 Skill。`tool_name` 必须逐字来自当前 ConversationSegment 中真实 `tool_call.name`，不得依赖工具注册表或由 LLM 翻译、概括、改名；候选必须存在同名且与真实调用 ID 对应的成功 `tool_result`。`failure_recovery` 还必须同时存在同名失败结果以及发生在其后的成功结果。其余知识字段允许按来源部分出现，但至少一个必须非空；用户或 Assistant 的口头描述不能冒充已验证知识，一次偶然成功不能被模型泛化成通用规律。
- `intentions/{intent_name}.md` 只保存 ConversationSegment 结束时仍未完成、未来仍需推进或等待触发的明确事项，并始终覆盖为最新状态快照，不追加执行历史。文件不重复保存 objective；`intent_name` 本身必须清楚表达事项。新建候选的状态只允许 `open`、`waiting`、`blocked`；`completed` 只能更新本次已经完整读取的旧 Intention，并必须逐字复用旧记忆的 `intent_name`，禁止新建一条已完成事项。`completed` 是保留文件但不再有效的终止状态并由默认召回排除。Intention 的业务状态只能由完整 Conversation、相关旧 Intention 和相关 Event 时间线中的明确事实决定；时间流逝不能自动完成、取消、删除、隐藏或停止召回。系统以 `last_confirmed_at` 记录最近一次由当前 Conversation 明确新建、更新或再次确认的时间；关系变化、索引刷新、L0/L1 重建、格式变化和无新语义重复写入都不得刷新它。默认 30、60、180 天只形成逐级增强的复核提醒，阈值必须由统一 Config 配置；提醒不改变 `status`，不触发定时 LLM 重写。Memory Editor 必须继续读取长期未确认事项，使后续明确事实可以更新或完成它。
- `memory/editor` 是新长期记忆编辑领域包，但旧数据契约仍然退出，不保留 `MemoryEditSource`、旧 `MemoryEditBatch` 或兼容入口。旧记忆读取已经确认为两层：`infrastructure/editor/snapshot` 提供领域无关的版本快照机制，`memory/snapshot.py` 只接受严格 L2 `memory://` URI 并读取完整 `MemoryDocument`。相关旧记忆检索采用 OpenViking 的编排思路：从完整 `ConversationSegment` 生成有界查询，按 Memory Schema 限定目录，固定文件直接读取，动态文件通过显式语义搜索契约返回 L2 URI，再预取有界 Top-N 完整快照；搜索失败、越界结果和完整性错误不得静默伪装为“没有旧记忆”。系统从本次完整旧记忆快照构造临时 `page_id` 映射，模型只输出尚未落盘的记忆候选、身份提议和关系候选；初步字段计划和二次审查完成后必须产生覆盖相关旧节点和本批候选的 `FinalIdentityMap`，明确每个编号最终是 CREATE、UPDATE、NOOP、MERGE 还是 DELETE，并绑定最终 L2 URI 或删除终态。多个编号可以指向同一最终 URI；已有旧编号只有在显式 MERGE 时才可重定向，DELETE 必须没有最终 URI。关系层不得自行猜测节点处置。
- 关系纯规划必须在 `FinalIdentityMap` 完成后执行：先将已有 Links/Backlinks 端点映射到最终身份，为 MERGE 迁移关系，为 DELETE 清理所有关系，再删除自环和重复项；然后校验并应用显式 `REMOVE`，最后幂等应用 `ADD`。`REMOVE` 的两端必须是本批完整读取的旧节点，并且规范关系在旧快照中真实存在；结构性节点清理不伪装为独立 REMOVE。相同规范关系不能在同一批次同时 ADD 和 REMOVE。对称的 `related_to` 与 `contradicts` 按 URI 顺序规范方向，所有关系统一按 `(from_uri, to_uri, link_type)` 去重。正向 Links 是关系计划的唯一输入，Backlinks 必须由最终正向集合确定性派生，不能由模型生成。未知编号、树外 URI、未完成身份、悬空端点和旧双向不一致都必须明确失败，不能像 OpenViking 一样静默跳过。
- 节点内容和关系不允许先后独立发布。Memory Editor 必须先在内存中构造全部最终 `MemoryDocument` 与删除集合，再使用一个 `MemoryCommitTransaction` 对受影响节点、关系端点和一跳邻居的 URI 并集稳定排序加锁，在锁内统一执行 state、revision 和完整内容摘要 CAS；同一文档的内容与关系变化合成一次写入且只推进一次 revision。事务必须先耐久记录 PREPARED 提交日志，再执行原子单文件替换和删除，完整回读后标记 COMMITTED；任一失败都在同一锁集内恢复全部旧文档并删除本批新文件。锁只解决并发，提交日志负责进程在多文件写入中途崩溃后的恢复。锁超时等尚未发布的瞬态失败可以有界重试；CAS 表明旧记忆语义已经变化时必须重新运行 Memory Editor，不能机械复用旧语义计划。L2 成功后再刷新可重建 L0/L1，刷新失败不回滚 L2。
- 节点字段变更继续采用独立于落盘的纯规划器：候选生成后先读取明确存在或缺失的精确目标快照，并按 YAML 形成初步 CREATE/UPDATE/NOOP 完整字段计划；该计划与候选、完整 Conversation 和完整旧快照一起进入第二遍语义审查，审查通过后再由确定性 `MemoryIdentityPlanner` 验证临时身份提议并生成 `FinalIdentityMap`。未读到的同址旧文件、未声明的旧编号改址、快照变化和类型不一致均明确失败。字段合并严格使用 YAML：`immutable` 冲突即失败；`patch` 接受候选给出的完整目标值，字段缺席时保留旧值，不接受 LLM `SEARCH/REPLACE`；`replace` 表示最新完整字段状态，缺席的可选字段从结果中移除；`add_only` 的已有节点只能完全一致，否则失败。MERGE 只能把一个完整读取且不再接收字段变更的旧节点并入同类、单跳且由字段计划覆盖的存活目标；默认只允许 preference、entity 和 intention，禁止 profile、tool 与 add-only event。DELETE 只能删除完整读取且不再接收字段变更的旧节点，原因必须是 `explicit_forget` 或 `fully_invalidated`；未提及、陈旧、低相似度、部分纠正、状态变化和已完成 Intention 都不构成删除。提议来源不得同时被关系候选引用，合并链、循环、多目标、退休目标和信息丢失必须失败。节点规划、关系规划和最终文档组装都必须是无加锁、无时间推进、无落盘副作用的纯步骤；原 `MemoryMutationCommitter` 和 `MemoryLinkCommitter` 的独立发布职责退出，只保留可被统一事务复用的快照、CAS、锁键、原子写入和回滚原语。
- `memory/semantic` 独立负责目录 L0/L1：使用受控目录快照生成覆盖全部直接子项的 L1，再从 L1 的简介确定性提取 L0，并按受影响目录链自下向上刷新。它不得读取 Conversation、决定 L2 内容或执行长期记忆合并。`memory/indexing` 负责把当前 L0/L1/L2 规范快照写入远程持久化向量索引，MemoryTree 始终是唯一真相源；Conversation Summary 使用另一个独立远程 Collection，只索引各 Conversation 当前不重叠的活跃摘要前沿，Summary 文件仍是唯一真相源，且该索引不能混入 Memory 排名、Memory `top_k` 或 `memory://` 地址空间。Segment Summary 由对应 MemoryJob 完成阶段同步；Range/Archive 父摘要耐久后必须先把该 Conversation 的远程记录切换到新活跃前沿，才能清理被覆盖来源。`infrastructure/vector/publication` 在任意 `RawVectorBackend` 上统一实现全量/增量发布、generation、checkpoint、building/ready、并发锁和失败重放，Adapter 只实现厂商的 Schema、认证、编解码、物理读写与索引可见性，工厂必须先构造 Raw Backend 再包装成后端无关 `VectorStore`。应用不得把向量退化为 SQLite 文件或进程内缓存。向量路由与 ModelClient 一样严格分离 `provider` 和 `adapter`：provider 只表示实际服务来源，工厂只按 adapter 构造协议实现，凭据值只通过 `credential_env` 从环境解析。当前首个正式 Adapter 是 `vikingdb`，同时覆盖火山引擎公共云 API Key、AK/SK 签名和私有化 Headers 三种认证；只有 AK/SK 管理模式可以创建或重建 Collection/Index，其他模式必须使用预创建的严格 Schema。完整 URI 和 attributes 保存为无损 text/JSON，VikingDB 的有限 string 标量索引只保存确定性哈希键；全量或增量发布必须等待远端 Index 读取到目标内容摘要并清除目标删除项后，才能把同 Collection 状态从 building 置为 ready 并推进 checkpoint。Qdrant 默认实现与配置已经退出，不保留双轨。以后接入其他向量数据库只能新增 Raw Backend、厂商严格配置和工厂注册，不得复制公共发布流程，也不得改写 Memory Editor、Agent SearchService 或索引领域契约。公共 `memory/retrieval` 采用参考 OpenViking、但严格适配当前记忆树的稳定分阶段检索架构：可配置 Embedder 是语义召回的必需依赖，每次查询只生成一次 query embedding；索引层只接受向量和受限 `memory://` 根目录，以记录的 `scope_roots` 做完整目录边界过滤；检索层支持直接 L2 向量召回，以及利用目录 L0/L1 的可配置层级召回；Reranker 是独立、可选的第二阶段，不得与 Embedding 混为一体。`kinds` 只限制 Top-K 前的直接 L2 主节点，不得截断其 Links/Backlinks 一跳关系；completed Intention 采用独立向量分区并由默认 Agent 召回在 Top-K 前排除，显式历史查询只允许通过 `MemoryIntentionRecallScope.COMPLETED`，Memory Editor 读取旧记忆则必须使用 `ALL`。`memory/editor/retrieval` 只保留从完整 `ConversationSegment` 选择 Schema 范围、固定 URI 和完整旧快照的写入专用编排，不得再拥有另一套索引或搜索引擎。无 Embedder 或远程向量服务不可用时必须明确失败，不能以关键词匹配或本地精确扫描冒充语义搜索。不得复活旧通用 Context、Catalog、FTS、Vector 或 Query 链，也不得在检索层引入租户、热度、另一套后台队列或旧数据兼容；关系候选只属于 Memory Editor，不能混入语义索引职责。
- 向量厂商配置必须遵循与 ModelClient 相同的扩展边界：`Config/root.py` 不得按 adapter 名称分支或 import 具体数据库；Runtime 只把统一的维度、记录容量、检索上限和单记录字符上限构造成 `VectorStoreRequirements`，由已注册 Adapter builder 结合自身严格配置校验。
- 仓库一级包 `Runtime/` 是当前记忆主链唯一组合根，只负责消费已校验配置、构造并共享 `ModelClient`、远程 `VectorStore`、Conversation、Agent SearchService、Memory Editor、语义刷新、MemoryJob Runner、MemoryWorker 与 LifecycleWorker，以及管理初始化、启动、停止和关闭状态；领域逻辑仍留在各自模块，`ModelClient/`、`pre/`、`memory/`、`infrastructure/` 和 `foundation/` 均不得反向导入 `Runtime`。仓库一级包 `Config/` 是模型、远程向量后端、切段、摘要、Conversation 生命周期、快照、Agent 检索、索引、解析、提交、语义刷新、Job 重试与 Worker 生命周期的唯一外部配置边界；`M2BOSConfig` 通过 `M2BOS_CONFIG_FILE` 指向的单一严格 YAML 文件加载，未知字段、重复键和宽松类型均必须拒绝，不保留 JSON 或分散环境字段兼容入口。配置只在启动时读取一次，不实现运行中热更新；模型与向量服务的秘密值都只按各自路由声明的环境变量名解析，YAML 不保存真实密钥。跨领域容量关系必须在组装前统一校验；各领域 Config 仍是内部强类型值对象，不得再建并行的配置加载入口或重复上限。Runtime 不使用进程全局单例、Service Locator、延迟 setter 注入、`atexit` 或构造函数中的存储/网络副作用；启动 Worker 前必须完成本地锁与记忆树初始化，并异步确认远程向量索引 ready；关闭时必须关闭远程客户端。没有未完成 Job 时，初始化阶段恢复孤儿事务；存在未完成 Job 时，只有先取得该最早 Job durable lease 的 Worker 才能恢复或检查其事务，且必须在任何业务执行前完成，未持有租约的 Runtime 不得触碰活跃 Worker 的 PREPARED 日志。MemoryWorker 在同一 memory-root 上固定单并发，认领必须使用耐久 `claim_id + claim_generation + worker_id + lease_expires_at` fencing；续约、完成和失败结算均须匹配同一租约，过期租约只能通过递增 generation 接管，陈旧 Worker 不得继续发布或改变 Job。LifecycleWorker 必须在 MemoryWorker 之后启动、在其之前停止，首次自动维护等待一个完整配置周期；每轮以 Conversation 根派生的全局耐久 lease 保证同一根只有一个维护者，续约失败必须取消本轮，按有界批量轮转全部 Conversation，并隔离单会话失败；轮转位置只以日期和 conversation_id 写入 `workflow/lifecycle/state.json`，不保存任何会话正文，并保证 Runtime 重启或维护者切换后不会总从最早目录重新开始。停止时先停止新生命周期周期和新 Job 认领，在各自心跳继续运行的情况下有界等待当前操作；超时才取消执行，并由下一次安全恢复或维护继续。重试时间必须以 `next_attempt_at` 耐久保存并采用有界退避，最终 FAILED 继续阻塞后续序号。
- 会随部署规模、模型成本、存储容量或等待策略变化的运行参数必须通过上述唯一 `Config/` 边界进入主链；领域 Config 类型继续与其行为实现放在同一领域模块，由 `Config/` 只负责严格聚合和 YAML 构造，禁止为了集中目录而让领域模块反向依赖配置组合根。统一示例 YAML 必须完整列出当前全部可调字段，不能把可调默认值隐藏在 Python 构造器中。Conversation journal 文件与枚举容量、MemoryTree 物理枚举容量、事务日志容量、Job/Receipt 文件容量以及 SQLite 锁等待参数均属于可调运维边界，不得继续作为运行路径中的散落常量；Schema 字段、URI/目录结构、page_id 范围、CAS/revision、MERGE/DELETE 资格、Links/Backlinks 一致性和事务发布顺序仍是不可配置的代码不变量。全局 `memory_sequence` 必须由 `jobs/state.json` 中永久保存且永不回退的 high-watermark 分配，不得再由仍存 Job 的最大值推导；先耐久推进高水位再创建 STAGED Job，崩溃只允许留下序号空洞，禁止重用。COMMITTED Job 默认保留30天，并且只有对应 History 已释放、COMMITTED Receipt 存在且没有 PREPARED/ROLLED_BACK Journal 时才可删除；FAILED、STAGED、QUEUED、RUNNING Job 不按时间删除。COMMITTED Receipt 默认保留365天，且只有 Job 已删除、History 已释放、序号不超过耐久高水位并且没有未完成事务时才可删除；PREPARED Receipt 不按时间删除。终态 Transaction Journal 在回执、语义刷新和 Job 完成后立即耐久删除，PREPARED Journal 必须先恢复、提交或完整回滚。
- Conversation 到记忆候选的结构化解析采用受控 ReAct：先执行一次现有的单查询语义检索，再由严格控制 Schema 每轮只选择 `finish`、一次 `memory_search` 或一次 `memory_read`；系统只执行这两种只读动作，并对重复查询、重复读取、总轮次、完整旧记忆数量、上下文字符数和工具结果大小作显式 Config 限制。Agent `SearchService.find()` 只执行当前问题的一次直接 Memory 查询，不调用充分性模型或 Summary 后备；`SearchService.search()` 可以参考 OpenViking，使用当前 Conversation 的活跃 Summary 前沿、最近 live 消息和受限目录 L0 生成有界多查询计划，并发召回后按 URI 确定性去重。这里当前 Conversation Summary 只帮助补全省略指代，不是历史后备召回。查询规划模型只能输出查询文本、简短意图和优先级，不能决定 URI、记忆类型、过滤器、分数或写入操作。主召回必须依次完成 Memory 候选、完整 L2 和 Links/Backlinks 一跳扩展；随后独立 Retrieval Grader 只判断这些 Memory 是否足以支持当前问题。只有判定 `insufficient` 时才以一条受控缺口查询检索少量历史 Conversation Summary，并在最终上下文中作为低权重后备区单独呈现；Summary 不参与 Memory 排名、不占用 Memory `top_k`、不能覆盖 Memory，Memory 充分时不得访问历史 Summary 索引。当前仍不实现 BM25/关键词召回、上下文化 Embedding 或知识图谱；真实 Reranker Provider 当前仅实现供应商无关的 `openai_compatible_rerank` 协议 Adapter，首个正式配置为阿里云 `qwen3-rerank`，凭据只允许通过 `DASHSCOPE_API_KEY` 环境变量注入。每次纳入旧 L2 节点时，可按其系统维护的 Links/Backlinks 有界补充一跳邻居；不得递归扩展、修改关系或把关系扩展混入向量索引职责。
- 受控 ReAct 中的 Retrieval Grader 只返回临时的充分性决策以及至多一个下一步查询或待读 URI，不产生或持久化置信度，也不能输出记忆候选、合并、删除或写入操作。上下文充分后，模型基于完整 `ConversationSegment`、所有完整旧 L2 快照和系统分配的临时 `page_id` 生成严格 `MemoryCandidateBatch`。领域校验与初步字段计划完成后，第二遍 Candidate 审查同时核对记忆候选、关系候选和身份提议，只允许 `accept`，或返回受控问题类型和简短说明的 `reject`；它必须检查 same-memory 身份是否成立、目标完整字段是否保留来源的仍有效事实，以及 remove-memory 是否确有完整 Conversation 支持，但不能修正候选、补充事实、决定 URI、生成最终 MERGE/DELETE、revision 或落盘。拒绝后只能在 Config 限制内携带问题重新生成完整候选批次；最终仍不通过时必须明确失败，禁止带病写入。
- Conversation 使用 `pre/conversation/messages` 与 `pre/conversation/summaries` 两层：messages 保存完整、严格区分 prompt、completion、tool_call、tool_result 的原始会话事实；每个 tool_result 必须发生在同 ID 的已知 tool_call 之后、逐字复用其 tool_name，并且一个调用只能保存一个终态结果，否则不得进入 Conversation。summaries 保存绑定不可变消息片段或连续摘要范围的宽语义历史过程，不能替代原文参与记忆解析。Segment Summary 与来源 Segment 一一绑定；Range Summary 只能覆盖同一 Conversation 中至少两份序号连续、同阶段且达到年龄与容量门槛的不可变摘要。第一阶段固定为 `SegmentSummary -> RangeSummary`，第二阶段固定为 `RangeSummary -> ArchiveRangeSummary`；ArchiveRangeSummary 不再自动压缩，禁止引入可配置无限层级或原地覆盖式滚动摘要。
- Conversation Schema 继续只放在 `pre/conversation`；旧 SessionArchive projector、旧本地文件读写和单会话覆盖式摘要已经退出。正式路径、live 追加、读取、history 封存、Segment Summary 和自动切段统一由 `memory/conversation` 实现，并复用通用 PathLock 与耐久原子文件能力；原子替换被强制中断后，只能忽略与该写入器严格命名规则吻合的有界普通临时文件，未知隐藏项和符号链接仍必须失败。锁的获取、fencing 入口与幂等 release 可以有界等待，但不得重放已经进入过的临界区正文，release 错误也不得覆盖正文原始异常。不得恢复旧入口或在 `pre` 中写存储逻辑。长期记忆写入只允许走 `MemoryJob -> Summary 与 MemoryEditor 纯规划 -> Summary 耐久化 -> MemoryChangeReceipt PREPARED -> MemoryCommitTransaction -> MemoryChangeReceipt COMMITTED -> L0/L1 refresh -> remote vector synchronize -> Job COMMITTED` 主链；向量 checkpoint 必须使用同一全局 `memory_sequence`，失败或中断时保持 building 并从 MemoryTree 重放，不得让半成品参与检索。周期压缩和跨层保留期删除只能走统一 Conversation 生命周期管理器。
- Conversation 自动封存采用 OpenViking 的 afterTurn 与 pending-token 思路：只有宿主明确进入 afterTurn、当前逻辑轮次以最终 completion 结束且该轮全部 tool_call 都有唯一终态 tool_result 时，才允许封存；默认 `commit_token_threshold=12000`、`keep_recent_turn_count=3`、`retained_message_token_budget=12000`、`max_segment_tokens=12000`，均从唯一 YAML 运维配置读取。正常大小的内容优先在完整轮次边界封存；单轮或单条文本超过上限时，先把超长 prompt/completion 无损拆成带 `logical_message_id + logical_part_index + logical_part_count` 的连续物理消息，再在 60%-100% Token 窗口内按完整轮次、完整 assistant step、完整消息、段落/句子语义块的顺序选择安全边界。tool_call 与对应终态 tool_result 永远不得被切开；Embedding 只为安全候选边界排序，调用失败必须确定性退回结构边界。每个结果仍是普通不可变 History Segment，并继续与同范围 Segment Summary 一一绑定；不得建立 checkpoint/oversized 旁路。显式操作只叫 flush/commit，不建立 OPEN/CLOSING/CLOSED 状态，也不阻止同一 conversation_id 后续继续追加。History Segment 继续由相同 conversation_id 与连续 sequence 确定性关联；不得让 LLM 生成这类结构关系。
- Conversation 生命周期主链为 `live.jsonl -> history/{segment_id}.jsonl -> summaries/{segment_id}.json`：消息先进入有界 live 窗口，完整片段封存为不可变 history，再从该 history 派生 summary。history 是有保留期的原文层，不是永久层；只有对应 summary 成功、通过来源绑定与结构校验并满足后续保留策略后，history 才可删除。history 与 segment summary 在原文保留期间通过 `conversation_id`、`segment_id` 和 `source_message_digest` 一一绑定，但 summary 在生成期间允许暂时不存在或处于失败状态。
- ConversationSegment 封存与 MemoryJob 之间必须使用两阶段耐久 outbox：在发布 history 前先创建不可执行的 `STAGED` Job 并分配 `memory_sequence`，history 原子发布且来源摘要校验通过后才能激活为 `QUEUED`；写入 STAGED 时可以继续持有 Conversation 租约，但不得嵌套持有 SQLite fencing 写事务，返回发布阶段前必须重新校验租约和完整 Segment 摘要。Worker 遇到最早的 `STAGED` Job 时必须从 Conversation live/history 幂等补完封存并核对 `segment_id` 与摘要，不能静默跳过。这样进程在“history 与任务创建”任一侧中断都不会造成 Segment 永久漏记。前台保存和后续对话不等待 LLM。所有写入同一 `memory-root` 的 Job 使用独立于 `conversation_id` 内消息序号的单调 `memory_sequence`，正常锁竞争必须使用显式配置的有界等待取得唯一序号；后台只有在前一序号的 Summary、最终 MemoryChangeReceipt、L2、L0/L1 和远程向量 checkpoint 全部耐久完成并标记 COMMITTED 后才能读取旧记忆并开始下一项，从而覆盖真正的跨 Conversation 顺序。MemoryChangeReceipt 只能由系统依据最终 Identity、关系计划和已提交事务日志生成，不能由 LLM 输出；最终文件只保存 URI、revision、digest、合并/删除处置和关系变化，不复制 Conversation 或记忆正文。当前没有 owner 层，排序范围就是单棵 memory-root；未来用户隔离只能在最外层把排序键扩展为 `owner_id + memory_root`。失败 Job 不得像 OpenViking 一样以终态跳过后继续处理后项，必须有界重试并保持后续 Job 阻塞，直到恢复或明确人工处置。RUNNING Job 的有效租约阻止其他 Worker 接管；租约过期后只能在耐久队列锁内递增 `claim_generation` 并产生新 claim，随后根据同一确定性事务的日志状态继续执行或补齐 Receipt、L0/L1、远程向量和 Job 终态。已 COMMITTED 的 L2 事务只能恢复完成，不能重新规划；陈旧 claim 的心跳、失败或完成结算全部拒绝。人工 `retry_failed()` 必须匹配当前完整失败版本，陈旧恢复对象不得覆盖后来 Worker 的状态。不同 memory-root 可以并行；同一 Job 内相互独立且只读同一不可变 Segment 的 Summary 与 MemoryEditor 纯规划可以并行，但两者都成功后才能发布任何 L2 变化。
- Segment Summary 不是无限累积的最终层。摘要压缩使用连续消息序号而不是固定日历分桶，年龄按来源覆盖结束时间计算而不是文件生成时间计算；默认 Segment Summary 至少7天且连续20份才可生成 RangeSummary，低频 Conversation 最老来源等待180天且至少有2份连续摘要时允许提前形成较小 RangeSummary；仅有1份时保留该 Segment Summary 作为语义终态，但通过相同工作流安全门槛后可以释放其 History。RangeSummary 至少180天且连续2份才可生成 ArchiveRangeSummary。每份父摘要保存来源 ID、摘要值和覆盖范围；父摘要耐久、完整回读并验证后，来源立即退出可重建的活跃检索前沿，再保留30天才允许物理删除。删除普通 RangeSummary 前必须先清理或确认不存在它覆盖的 Segment Summary，避免失去后续清理依据。领域执行器、跨层生命周期管理器、Runtime 显式单次入口和独立 LifecycleWorker 已经实现；LifecycleWorker 默认每3600秒取得全局 lease，以配置的有界批量轮转全部 Conversation，不能把维护循环混入 MemoryWorker。
- live 自动切段采用显式 Config 和纯规划器：按 `prompt` 锚点形成逻辑轮次，未完成最新轮次不得因容量阈值被封存；已闭合的超大逻辑轮次允许形成多个连续普通 Segment，Segment/Range Summary 必须保留 `starts_mid_turn` 与 `ends_mid_turn` 系统边界并按连续序号重新衔接语义。消息数、物理字节数、Token 预算、语义候选窗口、最近轮次保留数和单个 Segment 上限均由 Config 控制。超大文本工具结果在进入 Conversation 前只能转成有界 `summarized`，媒体、二进制、下载文件和无法安全概括的超大结果只能保存 `omitted` 描述、大小与摘要值，不得在 Conversation 或旁路 blob 中保存原始载荷。history 原文释放、summary 多周期压缩以及来源删除策略使用同一组生命周期 Config；Runtime 同时保留显式单 Conversation 维护入口和独立自动 LifecycleWorker，二者都只能调用统一 Conversation 生命周期管理器。
- SessionArchive、Behavior、ActionPolicy、Resource、Skill、通用 Context/ContextURI 及 `memoryos://` 协议已经整体退出，等待后续分别重构；必须同步删除其领域模型、存储、运行时、事务、SDK、HTTP、MCP、CLI 和集成调用者，不得保留别名、迁移器、禁用分支、占位实现或兼容入口。
- `pre.session`、旧 SessionArchive 文件布局、SourceStore、Context Catalog/FTS/Vector/Relation/Queue 等仅服务上述旧领域的实现一并退出。原子文件、路径安全和锁等确实被新 `memory/` 或 Conversation 独立使用的底层原语可以保留，但不得继续暴露旧领域语义。
- 新长期记忆只能以不可变 `ConversationSegment` 为事实来源，并且只能由按 memory-root 排序的 MemoryJob 调用 Memory Editor 自动生成或修改；其他入口不得绕过统一事务直接编辑 L2。
- 当前阶段不新增测试；新架构明确后重新设计测试体系。

- FAILED Job 的正式人工运维入口只能位于 Runtime：先读取当前阻塞队列的最早 FAILED 完整快照，再携带该快照调用 Store 的版本校验重试，不能按任意序号越过前序任务。Runtime 处于 RUNNING 时只允许在 MemoryWorker 已因该 Job 进入 BLOCKED 且无活跃执行后重试，成功后重新启动 Worker；READY 状态只重新开放 Job，不隐式启动 Worker。

## 稳定边界

- 模型能力统一由仓库一级包 `ModelClient/` 承载，不再以 `LLMClient/` 同时容纳对话、向量和重排。公共 `ProviderConfig` 必须分离厂商身份 `provider` 与调用协议 `adapter`，并保存模型、无凭据地址、密钥环境变量名和运行边界；chat、embedding、rerank 分别声明自己的能力配置。`ProviderFactory` 只按 `(capability, adapter)` 解析显式注册的 builder，不枚举模型名、不按厂商猜测协议、不静默回退；使用已有 adapter 的新厂商只能通过配置接入，只有出现全新请求/响应协议时才新增 adapter 代码。Embedding builder 只构造执行单次协议请求的 `EmbeddingProvider`，不得在厂商适配器内实现重试、批量调度或取得并发槽；`ProviderFactory.create_embedder()` 必须统一包装为 `EmbeddingClient`，由运行层负责输入校验、分批调度、可重试错误的指数退避和同一事件循环内跨实例共享的并发上限。第一个真实配置为 `provider=volcengine`、`adapter=ark_multimodal`、`capability=embedding`：通过通用异步 HTTP 传输调用方舟官方 `/embeddings/multimodal` REST 协议，把当前纯文本查询和记忆文档包装为 text part，校验并转换为配置维度的 `EmbeddingVector`；不得绑定厂商品牌 SDK，也不得因此保存或发送图片、音频、视频。Chat 保持同构的 `ChatClient -> ChatProvider` 边界，流式请求的重试语义仍由 Chat 运行层独立处理，不能为了复用 Embedding 代码而混淆两种能力。`openai_compatible_chat` 是新的 Chat Completions 协议适配器：只负责消息、工具、结构化输出、推理参数、普通响应和 SSE 事件的协议转换以及单次 HTTP 调用，不得内置重试、并发调度或按厂商名称分支；DeepSeek 作为首个真实配置，其他使用相同协议的厂商只更换配置。结构化输出能力必须显式声明为 `none`、`json_object` 或 `json_schema`，不能用布尔值掩盖协议差异。Claude Messages 等不同协议继续等待后续逐项实现；旧 `LLMClient/`、LiteLLM/OpenAI-compatible 实现及兼容导入必须退出，旧 `infrastructure/model` 路径也不得恢复。
- 结构化输出采用分层容错：可以从代码块或外围文本提取 JSON，并可修复尾逗号、单引号等纯语法问题；修复只针对提取出的 JSON 候选，修复结果仍必须通过完整 JSON Schema 和领域模型校验。禁止把 list/dict/number 自动转成 string、把 string/dict 自动包成 list、把非法枚举替换为默认值、忽略未知字段、补造必填值或猜测业务内容。解析、JSON Schema、领域校验失败必须区分阶段，并把精确错误反馈给模型做有界重试；重试提示要求保留已经合法的值，不得通过有损类型转换规避校验。超过重试次数后返回明确失败，不生成结构化结果。
- 通用 Context、ContextURI、ContextProjectionSanitizer 以及旧 Catalog/FTS/Vector/Trace/L0/L1 链路已经退出，不得被新记忆树复用或恢复。
- Behavior、ActionPolicy、SessionArchive、Resource 和 Skill 当前没有公开入口或运行时主链；后续重构必须作为新设计另行确认。
- Python 发行名称统一为 `m2bos`；旧 CLI、环境变量协议和 `memoryos://` URI scheme 不再构成稳定契约。

## 工作区安全

- 修改前记录分支、HEAD、工作区状态、未跟踪文件和相关公开入口。
- 用户已有修改不得回退、覆盖或顺手格式化。
- 不自动提交、不自动 Push；README 仅在用户明确授权时修改。
- 源码中的说明性注释和 docstring 统一使用中文；许可证声明、协议名称、公开标识符以及 `noqa`、`type: ignore` 等机器指令保持原样。
- 调整契约时检查所有调用者、运行时装配、Schema、Worker、SDK、HTTP、MCP、CLI 和外部集成。

## 当前验证门槛

旧测试资产已经退役。新测试体系建立前，完成生产源码变更至少运行：

- Python compile；
- Ruff；
- MyPy；
- Pyright；
- 与改动相关的真实运行时或存储 smoke check。

工具缺失或外部服务不可用时记录真实错误，不得伪造通过，也不得为运行检查随意修改依赖。
